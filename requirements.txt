# --- API / Server ---
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.32

# --- LLM / Agent orchestration ---
langchain>=0.1.9
langchain-community>=0.0.22
langchain-core>=0.1.23
langgraph>=0.0.40
langchain-openai>=0.0.5      # optional OpenAI provider
langchain-ollama>=0.1.0      # primary provider (llama3.2:3b)

# --- Retrieval (Hybrid RAG) ---
faiss-cpu>=1.8.0             # dense retriever
# BM25 is implemented in-repo (rag/retriever.py) — rank-bm25 0.2.2 is buggy
# under numpy 2.x, so we avoid the dependency.
sentence-transformers>=2.5  # embeddings + reranker (BAAI/bge-reranker)

# --- Document parsing ---
PyMuPDF>=1.24

# --- Tools ---
duckduckgo-search>=5.0       # web search (no API key needed)
notion-client>=2.2           # Notion integration

# --- Config / persistence ---
pydantic>=2.6
pydantic-settings>=2.1
SQLAlchemy>=2.0
python-dotenv>=1.0
httpx>=0.27
tabulate>=0.9

# --- Dev ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.3
